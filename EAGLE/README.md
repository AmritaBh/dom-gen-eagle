For training, use the `multi_domain_runner.py` file after setting params and paths as needed. Also change the loss weights in `eagle_train.py` as needed before training.

Evaluate using `evaluation.py`.
